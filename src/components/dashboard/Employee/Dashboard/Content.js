import classes from './Content.module.css';

const EmployeeDashboard = () => {
  return (
    <section className={classes.starting}>
      <h1>Welcome on Employee Dashboard!</h1>
    </section>
  );
};

export default EmployeeDashboard;
