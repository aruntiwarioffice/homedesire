import classes from './Content.module.css';

const PropertyContent = () => {
  return (
    <section className={classes.starting}>
      <h1>Welcome on Property!</h1>
    </section>
  );
};

export default PropertyContent;
