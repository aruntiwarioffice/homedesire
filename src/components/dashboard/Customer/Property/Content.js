import classes from './Content.module.css';

const CustomerProperty = () => {
  return (
    <section className={classes.starting}>
      <h1>Welcome on Customer Property!</h1>
    </section>
  );
};

export default CustomerProperty;