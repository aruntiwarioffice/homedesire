import classes from './Content.module.css';

const ContactUsContent = () => {
  return (
    <section className={classes.starting}>
      <h1>Welcome on Contact Us!</h1>
    </section>
  );
};

export default ContactUsContent;
