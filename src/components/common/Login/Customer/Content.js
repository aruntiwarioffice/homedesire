import classes from './Content.module.css';

const CustomerLogin = () => {
  return (
    <section className={classes.starting}>
      <h1>Welcome on Customer Login!</h1>
    </section>
  );
};

export default CustomerLogin;
