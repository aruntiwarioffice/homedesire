import classes from './Content.module.css';

const EmployeeProperty = () => {
  return (
    <section className={classes.starting}>
      <h1>Welcome on Employee Property!</h1>
    </section>
  );
};

export default EmployeeProperty;
