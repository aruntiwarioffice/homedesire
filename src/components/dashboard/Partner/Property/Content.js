import classes from './Content.module.css';

const PartnerProperty = () => {
  return (
    <section className={classes.starting}>
      <h1>Welcome on Partner Property!</h1>
    </section>
  );
};

export default PartnerProperty;
