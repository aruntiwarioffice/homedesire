import classes from './Content.module.css';

const CustomerRegister = () => {
  return (
    <section className={classes.starting}>
      <h1>Welcome on Customer Register!</h1>
    </section>
  );
};

export default CustomerRegister;
